<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>ReadableApi · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/assets/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/assets/app.js") %>"></script>
    <style>
      .header {
          color: #DC2626;
          font-size: 4rem;
          text-align: center;
          text-decoration: underline;
        }
    </style>
  </head>
  <body>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <h1 class="header">Readable</h1>
      <section>
        <p style="text-align: center">
        <%= if !@already_confirmed do %>
          Thank your for verifying your email. Click <a href="<%= @url %>">here</a> to open the application and get started!
        <% end %>
        <%= if @already_confirmed do %>
          It appears this account has already been verified. Click <a href="<%= @url %>">here</a> to open the application and get started!
        <% end %>

        </p>
      </section
    </main>
  </body>
</html>
